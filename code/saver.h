#include <iostream>

// Aqui se procesa la informacion de la simulacion y se guarda en un JSON para poder reejecutar la simulacion en un futuro

// CLass Saver - Atributos: simulation, inventory, order, queue | Metodos: Constructor, setters y getters, save(), load(), clear()

class Saver
{
    private: 
        
    public:
        Saver(){}
        // Setters

        // Getters

        void save()
        {
            // Aqui se guarda la informacion de la simulacion en un JSON
        }
        void load()
        {
            // Aqui se carga la informacion de la simulacion en un JSON
        }
        void clear()
        {
            // Aqui se limpia la informacion de la simulacion en un JSON
        }
};